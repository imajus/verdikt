{
  "name": "@verdikt/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Marketplace dashboard — the primary demo surface",
  "comment": "Lit owns the application view and event bindings while the chain-facing modules remain framework-independent.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cf:dev": "vite build && wrangler dev",
    "deploy": "vite build && wrangler deploy"
  },
  "dependencies": {
    "@awesome.me/webawesome": "^3.12.0",
    "@verdikt/cre": "workspace:*",
    "@verdikt/sdk": "workspace:*",
    "@verdikt/sla": "workspace:*",
    "@web3-onboard/core": "^2.24.1",
    "@web3-onboard/injected-wallets": "^2.11.3",
    "lit": "^3.3.3",
    "viem": "^2.56.3"
  },
  "devDependencies": {
    "@lit-labs/ssr": "^4.1.0",
    "@verdikt/fixtures": "workspace:*",
    "vite": "^8.2.2",
    "wrangler": "^4.129.0"
  }
}
