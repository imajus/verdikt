# The spike calls a public endpoint, so it needs no secret. The real
# per-request workflow will need the provider request credentials here —
# released by the Vault DON directly into the enclave, never through
# usingTheDons().
secretsNames: {}
