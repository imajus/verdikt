# Empty, and currently unreferenced: both workflow.yaml files set
# `secrets-path: ""`, so nothing loads this yet.
#
# It is here because the per-request workflow has a known, specified need for it.
# Specification.md §2 requires the enclave to replay the agent's payment "with
# request credentials kept encrypted in-enclave". A provider whose API needs an
# upstream key — rather than the public endpoint the demo uses — supplies it
# here, and the Vault DON releases it directly into the enclave. Never through
# `usingTheDons()`: that path crosses the DON boundary, which is the one thing a
# credential must not do.
#
# Add the name here and set `secrets-path: "../secrets.yaml"` in the workflow's
# own workflow.yaml when that lands.
secretsNames: {}
