{
  "name": "@verdikt/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Spikes and operational scripts — throwaway by design, kept for the evidence they produce",
  "scripts": {
    "spike:ens": "node --env-file-if-exists=../.env spike-ens.mjs",
    "setup:ens": "node --env-file-if-exists=../.env setup-ens.mjs"
  },
  "dependencies": {
    "@verdikt/sdk": "workspace:*",
    "viem": "^2.56.3"
  }
}
